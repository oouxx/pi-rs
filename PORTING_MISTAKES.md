# PORTING_MISTAKES.md — 移植错误归档

对齐检查中修复的"回归 bug"（不是有意保留的偏差）记录于此。根因模式
尽量归到 `PORTING.md` 阶段一"高危陷阱"表已有的分类；新出现的模式
同时补回 `PORTING.md` 的陷阱表。

| 位置 | 现象 | 根因模式 | 修复方式 |
| ---- | ---- | -------- | -------- |
| `pi-coding-agent/src/core/extensions/loader.rs` `discover_extensions_in_dir` | `pi install` 装完的扩展（symlink 到 `agent_dir/extensions/`）在会话启动时不被发现，扩展工具不可用 | **隐式行为依赖**：TS 用 `entry.isDirectory() \|\| entry.isSymbolicLink()` 把 symlink 按目标类型分类；Rust 的 `entry.file_type()` 对 symlink 返回 link 自身（`is_dir=false, is_file=false`），原代码的 `.or_else(metadata)` 兜底只在 `file_type()` 失败时触发，symlink 场景永远不触发，导致 symlink 目录/文件被静默跳过 | 当 `file_type()` 报告 `is_symlink()` 时显式用 `std::fs::metadata()` 跟随目标分类；补 3 个测试（symlink 目录、symlink 文件、悬空 symlink） |
| `pi-coding-agent/src/core/extensions/js_runtime.rs` `TypescriptModuleLoader::resolve` | 真实 npm 扩展（如 `@narumitw/pi-goal`）用 `.js` 后缀 import（`import ... from "./goal.js"`，TS ESM 标准写法），文件在磁盘上是 `.ts`，加载报 "No such file or directory" | **隐式行为依赖**：TS 原版用 jiti 加载，jiti 自动做 `.js`→`.ts` 扩展名替换；Rust 的 `resolve_import` 只做字面解析，不尝试替换 | 在 `resolve` 步骤加 jiti 式扩展名回退：`.js`→`.ts`/`.tsx`/`.jsx`、`.mjs`→`.mts`、`.cjs`→`.cts`、目录→`index.*`；补测试 |
| `pi-coding-agent/src/core/package_manager.rs` `install` | `pi install foo`（裸名）被直接传给 `npm install`，而 TS 原版把裸名当本地路径（不存在则报 "Path does not exist"） | **缺失前置检查**：TS `install()` 先 `parseSource` 再按类型分发（npm/git/local），local 分支做 `existsSync` 校验；Rust 原实现不解析直接跑 npm | `install` 改为 `parse_source` 分发：npm→npm install（spec 去掉 `npm:` 前缀）、git→git clone、local→resolve+exists 校验；CLI 改调 `install_and_persist`（TS `installAndPersist`），默认 scope 从 project 改为 user（TS `local=false`） |
| `pi-coding-agent/src/core/extensions/bun/bootstrap.ts` `createCtx()` | plan-mode 扩展 `session_start` handler 调 `ctx.sessionManager.getBranch()` 后直接 `branch.length`，报 "undefined is not an object" | **隐式行为依赖**：TS 原版 `getBranch()` 永远返回 `SessionEntry[]`（无会话时返回空数组）；Rust stub 返回 `undefined`，扩展依赖"返回数组"的隐式契约 | `getBranch` 改为返回 `[]`；同时把整个 sessionManager 从硬编码 stub 升级为宿主随事件推送的快照缓存（见 DEVIATIONS.md 新条目） |
| `pi-coding-agent/src/core/extensions/bun/bootstrap.ts` `pi.getActiveTools()` 等 | plan-mode 扩展 `safeGetActiveTools()` 用 `try/catch` 包 `pi.getActiveTools()` 后直接 `.filter()`，报 "toolNames.filter is not a function" | **同步/异步契约错位**：TS 原版 `getActiveTools(): string[]`/`getCommands()`/`getThinkingLevel()`/`getSessionName()` 是**同步**读 API（直接返回数组/字符串）；Rust bootstrap 把它们实现成异步 RPC（返回 Promise），`try/catch` 抓不住 Promise rejection，拿到 Promise 当数组用 | 同步读 API 全部改为从宿主推送的快照缓存同步读取（`getActiveTools`/`getAllTools`/`getCommands`/`getThinkingLevel`/`getSessionName`/`ctx.model`/`ctx.thinkingLevel`）；写 API（sendMessage/appendEntry/setActiveTools 等）保持异步 RPC |
| `pi-coding-agent/src/core/session_manager.rs` `SessionEntry` serde | 快照/JSONL 里 entry 字段输出 `parent_id`/`custom_type` 等 snake_case，扩展读 `parentId`/`customType` 全是 undefined——plan-mode 的 `candidate.customType === stateEntryType` 检查**静默失败**（不崩但状态恢复功能是坏的） | **serde rename_all 陷阱**：`#[serde(rename_all = "camelCase")]` 在 enum 上**只作用于 variant 名，不作用于字段名**；字段名需要 `rename_all_fields`（serde ≥1.0.186） | 加 `#[serde(rename_all_fields = "camelCase")]`；同时给每个字段加 `#[serde(alias = "snake_case")]` 兼容旧 JSONL 反序列化（序列化统一 camelCase，与 TS 一致） |
| `pi-coding-agent/src/core/extensions/bun/bootstrap.ts` `createCtx()` sessionManager | 扩展调用 `ctx.sessionManager.getLeafId()`/`getEntry()`/`getLabel()`/`getTree()`/`getHeader()`/`getEntries()`/`buildContextEntries()`/`getLeafEntry()` 报 "undefined is not a function" | **方法面缺失**：TS 原版 `ReadonlySessionManager` 有 14 个方法，bootstrap 只实现了 6 个（getCwd/getSessionDir/getSessionId/getSessionFile/getSessionName/getBranch） | 快照扩展为带 `leafId`/`header`/`entries`/`labels`，bootstrap 从 entries 派生全部 14 个方法（getBranch 支持 fromId、getTree 从 parentId 建树、buildContextEntries 复刻 compaction 逻辑） |
