# Clippy configuration for pi-rs.
# Thresholds tuned to match the codebase patterns.

cognitive-complexity-threshold = 30

too-many-arguments-threshold = 10

too-many-lines-threshold = 200

array-size-threshold = 65536
