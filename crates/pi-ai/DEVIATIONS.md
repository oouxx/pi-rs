# DEVIATIONS.md — pi-ai

按 CLAUDE.md 阶段四的偏差日志格式记录。对齐检查遇到下列差异时按对应
"确认状态"处理。

| # | 位置（文件/函数） | 原 TS 行为 | Rust 实际行为 | 修改原因 | 确认状态 |
|---|------------------|-----------|---------------|----------|----------|
| 1 | 模型生成入口 `crates/xtask` / `crates/pi-ai/build.rs` / `data/models_generated.json` | `scripts/generate-models.ts` + `scripts/generate-image-models.ts` 作为 pre-publish 脚本抓取并生成 `src/providers/*.models.ts` + `src/models.generated.ts`，产物入仓，下游零网络 | 维护者跑 `cargo run -p xtask -- generate-models` 抓取并写盘 `crates/pi-ai/data/models_generated.json`，提交进仓；`build.rs` 不联网，运行时 `include_str!` 嵌入 | 对齐原版"生成是维护者动作、产物入仓、下游零网络、可复现"的设计；避免每次编译联网、离线构建静默退化成 0 模型的隐患 | 已确认保留 |
| 2 | 模型来源/覆盖度 `xtask/src/generate_models.rs::process_models_dev` 等 | `generate-models.ts` 覆盖 models.dev 的全部 provider（含 amazon-bedrock、google-vertex、kimi-coding、moonshotai/-cn、xiaomi/token-plan-*、opencode/-go、nvidia），外加 Vercel AI Gateway、NVIDIA NIM、`generate-image-models.ts` 图像模型，以及大批手工补充模型与 compat/thinking 元数据计算 | 当前仅覆盖 13 个 provider（anthropic/openai/google/deepseek/groq/cerebras/xai/mistral/together/fireworks-ai/github-copilot/minimax/minimax-cn）+ OpenRouter，无 AI Gateway/NIM/图像模型，无手工补充模型，compat/thinkingLevelMap/headers/tiers 等元数据基本未生成 | **用户拍板：主流支持仅需 anthropic + openai 格式，不扩展其他 provider 后端（azure/codex/bedrock/vertex/kimi/vercel 等均不移植）**；现有 13 provider 覆盖即最终范围 | 已确认保留 |

| 3 | `mistral-conversations` API / mistral provider | 原版有独立 `mistral-conversations` API 实现（官方 Mistral Conversations API + 消息转换 + tool-call id 归一化）及 `mistral` provider 模型 | 已删除：不再注册 `mistral-conversations` 后端，不再生成 `mistral` provider 模型，移除 `MISTRAL_API_KEY` 映射与默认模型/display name | 用户反馈 Mistral 使用面小、且原 Rust 实现是错误路由到 openai-completions（行为与原版不符）；与其保留错误实现，不如显式移除。需要时按原版补完整 `mistral-conversations` 后端再恢复 | 已确认保留 |
| 4 | `ollama` provider / `providers/ollama.rs`、`env_api_keys.rs`、`providers/openai.rs` | 原版无内置 Ollama 支持（用户须自行在 `models.json` 配 OpenAI 兼容条目指向 Ollama） | 新增运行时自动发现：`discover_ollama_models()` 探测本机 `http://localhost:11434/api/tags`，把已安装模型注册为 `api=openai-completions` 的 `ollama` provider 模型；探测失败静默返回空（Ollama 未运行属合法状态）；端点可用 `OLLAMA_BASE_URL`/`OLLAMA_HOST` 覆盖。鉴权：`ollama` provider 现映射 `OLLAMA_API_KEY` 环境变量；若该变量已设则作为 `Authorization: Bearer <key>` 携带，未设则**不携带** Authorization header（`openai.rs::stream_openai_inner` 对空 key 不再发空 Bearer） | 本地 LLM 是 pi-rs 的常用场景，自动发现降低使用门槛；原版 `models.json` 路径仍可用（且可覆盖自动发现的默认值，如 contextWindow）。Ollama 经反向代理加鉴权时可用 `OLLAMA_API_KEY`；本地裸跑无需设 | 已确认保留（pi-rs 增强，非原版行为） |

> 备注：#2 属于"未完成"而非"有意偏差"。补齐时按原版逻辑逐项移植，并
> 在补完后把对应条目状态改为"已确认保留"或删除。
| 5 | `openai-responses` 后端 `providers/openai_responses.rs` | 原版 `openai-responses.ts` + `openai-responses-shared.ts` 含：grammar 约束采样（`custom_tool_call` + grammar JSON buffer）、deferred tools（`tool_search_call`/`tool_search_output`）、service tier 定价（flex/priority 倍率）、GitHub Copilot 动态 headers、provider retry、`rawStopReason` 字段、`sessionAffinityFormat`/`supportsDeveloperRole` 等 compat 字段 | 已实现：消息/工具转换（tool-call id 归一化、text signature、reasoning 回放）、SSE 流式解析（thinking/text/toolcall 事件序列）、usage/stop-reason 映射、`rawStopReason`、service tier 定价（flex/priority 倍率）、`sessionAffinityFormat`/`supportsDeveloperRole`/`supportsStrictMode`/`supportsLongCacheRetention` compat（#14）、session affinity headers（openrouter→x-session-id、openai→session_id+x-client-request-id、openai-nosession→x-client-request-id）、provider retry（`retryProviderRequest` 等价：x-should-retry/retry-after 头、指数退避 + jitter、abortable sleep）、GitHub Copilot 动态 headers（X-Initiator/Openai-Intent/Copilot-Vision-Request）、grammar 约束采样、deferred tools | 核心行为与原版一致。grammar 约束采样已移植：`Tool.constrained_sampling` 字段 + `GrammarVariants`/`ConstrainedSamplingConfig`/`JsonSchemaStrict` 类型 + `resolve_*`/`infer_grammar_input_property`/`get_grammar_tool_input` + 请求侧 `custom_tool_call`/`custom_tool_call_output` 转换 + 流式侧 `response.custom_tool_call_input.delta/.done` 与 `GrammarToolInputJsonBuffer` 增量重建（含 `test_grammar_input_delta_append_only`/`test_process_stream_custom_tool_call_input` 对齐）。deferred tools 已移植：`split_deferred_tools` + `tool_search_call`/`tool_search_output` 生成（deferred 工具带 `defer_loading:true`，对齐 TS）。已修与 TS 的差异：① 非法 grammar 输入由"静默吞掉发空串"改为**传播错误**（`convert_responses_messages` 返回 `Result`，`get_grammar_tool_input` 的 Err 冒泡）；② tool-call id 归一化改为仅对**不同模型**消息生效（对齐 TS `transformMessages` 的 isSameModel 门控 + toolCallIdMap），同模型保留原始 id；③ custom_tool_call 的 id 丢弃条件改为对齐 TS 公式 `(!isSameModel && fc_) || (!grammar && !fc_)`。注意：① `resolve_grammar_constrained_sampling` 仍未包裹 `infer_grammar_input_property` 的错误（TS 用 try/catch 前缀 `Tool "X" cannot use grammar constrained sampling: `），仅在错误信息措辞上有差异；② `Tool.constrained_sampling` 与 `Message.ToolResult.added_tool_names` 目前无数据源写入（pi-rs 扩展 API 仍未暴露 `addedToolNames`，见 `js_adapter.rs`），属已就绪的潜伏能力 | 已确认保留 |
| 6 | `openai-completions` 后端 `providers/openai.rs` | 原版 `openai-completions.ts` 含：`detectCompat`/`getCompat` 运行时 compat 解析（按 provider/baseUrl 推断 + 显式 compat 覆盖）、请求构建（system/developer 角色、store、max_tokens vs max_completion_tokens、stream_options 条件、reasoning_effort + 9 种 thinkingFormat、chat_template_kwargs/args、thinking_token_budget、prompt_cache_key/retention、session affinity headers、kimi deferred tools、zai_tool_stream、tool history 空 tools、anthropic cache control、OpenRouter routing）、消息转换（thinking → signature 字段/reasoning_details、requires_* 标志、图片、grammar custom tools） | 已移植：`detect_compat`/`get_compat`（zai/together/moonshot/openrouter/cloudflare/nvidia/ant-ling/deepseek/chutes/cerebras/opencode 探测，thinkingFormat/maxTokensField/sessionAffinity/cacheControlFormat 推断）、请求构建全项、消息转换全项、`map_stop_reason` 对齐（未知 reason → Error + `Provider finish_reason: ...`）、`chunk.model` → `responseModel`、`StreamOptions.reasoning_effort`/`thinking_budgets` 透传。残留差异：① `vercelGatewayRouting` 已解析但未用于请求（无 Vercel provider 数据源）；② `supportsFinishReason` 已解析但流式循环未按它调整收尾判断（Rust 始终跟踪 has_finish_reason）；③ `requiresThinkingAsText`/`requiresToolResultName` 已接入消息转换 | 核心行为与原版一致；残留差异均为已就绪的潜伏能力或低影响路径 | 已确认保留 |
| 7 | `pi-messages` 后端 `providers/pi_messages.rs` | 原版 `api/pi-messages.ts`：POST `{model, context, options}` 到 `<baseUrl>/messages`，SSE **流式**读取（边读边解析）序列化事件，支持 `debug=1` 查询参数、`PiMessagesResponseError`（结构化 diagnosticDetails）与结构化诊断（rewrite / response_failure） | **已按原版对齐**：SSE 流式逐块解析（`bytes_stream` + 增量 buffer，对齐 `readPiMessagesEvents`）、`?debug=1`（`StreamOptions.debug`/`SimpleStreamOptions.debug`）、结构化 `AssistantMessageDiagnostic`（`{type,timestamp,error?,details?}`，从简化形态升级为 TS 结构化形态，`DiagnosticSeverity` 移除）、`PiMessagesResponseError`（code + diagnosticDetails：version/provider/model/url/status/statusText/error|body/timestampMs）、rewrite 诊断 `pi_messages_rewrite` + 错误诊断 `pi_messages_response_failure`（`extractDiagnosticError` 等价） | 核心协议与错误/诊断行为与原版一致 | 已确认保留 |
