# PORTING_MISTAKES.md — pi-coding-agent RPC Mode

This file documents bugs found and fixed during the RPC mode alignment check.

| # | 位置 | 现象 | 根因模式 | 修复方式 |
|---|------|------|---------|---------|
| 1 | `handler.rs:Prompt` | Prompt handler returned `None` — RPC client never got a response for prompt commands. | Missing success/error response emission. TS emits response via `preflightResult` callback; Rust had no equivalent. | Added success response emission in `AgentEnd` event listener. |
| 2 | `handler.rs:Prompt` | `id` field was discarded (`id: _`), so prompt responses had no correlation ID. | Field ignored in pattern match. | Changed to use `id` for the response. |
| 3 | `handler.rs:Prompt` | `images` and `streaming_behavior` fields were discarded. | Fields ignored in pattern match. | Passed through to `PromptOptions`. |
| 4 | `handler.rs:AbortBash` | `AbortBash` called `session.abort()` instead of `session.abort_bash()`. | Wrong method called — TS has separate `abortBash()` and `abort()` methods. | Changed to `session.abort_bash()`. |
| 5 | `handler.rs:AbortRetry` | `AbortRetry` called `session.abort()` instead of `session.abort_retry()`. | Wrong method called — TS has separate `abortRetry()` and `abort()` methods. | Changed to `session.abort_retry()`. |
| 6 | `handler.rs:SetAutoRetry` | `set_auto_retry` was a no-op — returned success but didn't call `session.set_auto_retry_enabled()`. | Implementation missing. | Added call to `session.set_auto_retry_enabled()`. |
| 7 | `handler.rs:SetSteeringMode` | `set_steering_mode` was a no-op — returned success but didn't call `session.set_steering_mode()`. | Implementation missing. | Added call to `session.set_steering_mode()`. |
| 8 | `handler.rs:SetFollowUpMode` | `set_follow_up_mode` was a no-op — returned success but didn't call `session.set_follow_up_mode()`. | Implementation missing. | Added call to `session.set_follow_up_mode()`. |
| 9 | `handler.rs:GetState` | `model` field was a string (model ID) instead of a full model object. | Response format mismatch — TS returns full `Model<any>` object. | Changed to return `RpcModelInfo` with id, provider, name, api. |
| 10 | `handler.rs:SetModel` | Response returned `{id, provider}` instead of full model object. | Response format mismatch. | Changed to return full model via `serde_json::to_value(&m)`. |
| 11 | `handler.rs:CycleModel` | Response always returned `{model: {id, provider}}` — never `null`. | Response format mismatch — TS returns `{model, thinkingLevel, isScoped} | null`. | Changed to use `session.cycle_model()` which returns `Option<(Model, Option<ThinkingLevel>, bool)>`. |
| 12 | `handler.rs:CycleThinkingLevel` | Response always returned `{level}` — never `null`. | Response format mismatch — TS returns `{level} | null`. | Changed to use `session.cycle_thinking_level()` which returns `Option<String>`. |
| 13 | `handler.rs:SetSessionName` | No validation — empty names were accepted. | Missing validation — TS validates `name.trim()` is non-empty. | Added validation and error response for empty names. |
| 14 | `handler.rs:GetEntries` | Returned `{id, type: "entry"}` stubs instead of real `SessionEntry` objects. | Wrong data returned — used `session_entry_id()` helper instead of serializing entries directly. | Changed to serialize `SessionEntry` via `serde_json::to_value()`. |
| 15 | `handler.rs:GetTree` | Returned flat entry stubs instead of tree structure. | Wrong data returned — didn't use `session.get_tree()`. | Changed to use `session.get_session_manager().get_tree()` and serialize recursively. |
| 16 | `handler.rs:GetForkMessages` | Returned full `AgentMessage::User` serialized instead of `{entryId, text}` format. | Response format mismatch. | Changed to use `session.get_user_messages_for_forking()` which returns `Vec<(String, String)>`. |
| 17 | `handler.rs:Clone` | Response returned `{path, cancelled}` instead of `{cancelled}`. | Response format mismatch — TS returns `{cancelled}`. | Changed to return `{cancelled}` only. |
| 18 | `handler.rs:Fork` | Response returned `{path, cancelled}` instead of `{text, cancelled}`. | Response format mismatch — TS returns `{text, cancelled}`. | Changed to return `{text, cancelled}` (text is empty string as noted in DEVIATIONS.md). |
| 19 | `rpc_types.rs` | `RpcCommand` enum was missing `ExportHtml` variant. | Missing variant — TS has `export_html` in `RpcCommand` type. | Added `ExportHtml` variant. |
| 20 | `mod.rs` | Event channel and direct stdout writes could interleave, corrupting JSONL protocol. | Two writers to stdout — spawned task for events and `print!()` for responses. | Changed to single `mpsc::UnboundedSender<String>` channel for all stdout writes. |
| 21 | `mod.rs` | Signal handler took ownership of session via `session_for_signal`, then main loop also used session — race condition. | Double-use of session between signal handler and main loop. | Changed to use `AtomicBool` flag for shutdown signaling; main loop handles cleanup. |

| # | 位置 | 现象 | 根因模式 | 修复方式 |
|---|------|------|---------|---------|
| 22 | `handler.rs:Compact` | Compact response returned `{"compacted": true, "summary": summary}` instead of full `CompactionResult` object | Response format mismatch — TS returns `CompactionResult` with `summary`, `firstKeptEntryId`, `tokensBefore`, `estimatedTokensAfter`, `details` | Changed `compact()` to return `CompactionResult` struct; handler serializes it directly |
| 23 | `compaction.rs:CompactionResult` | `CompactionResult` was missing `estimatedTokensAfter` field | Missing field — TS `CompactionResult` has `estimatedTokensAfter?: number` | Added `estimated_tokens_after: Option<u64>` with `#[serde(skip_serializing_if = "Option::is_none")]` |
| 24 | `handler.rs:GetState` | `model` field returned `RpcModelInfo` (subset) instead of full `Model` object | Response format mismatch — TS returns full `Model<any>` object | Changed `RpcSessionState.model` to use `pi_agent_core::pi_ai_types::Model` directly |

| # | 位置 | 现象 | 根因模式 | 修复方式 |
|---|------|------|---------|---------|
| 25 | `mod.rs:signal_handler` | Signal handler didn't call `kill_tracked_detached_children()` before shutdown | Missing behavior — TS `registerSignalHandlers()` calls `killTrackedDetachedChildren()` on signal | Added `crate::utils::shell::kill_tracked_detached_children()` call in signal cleanup path |

| # | 位置 | 现象 | 根因模式 | 修复方式 |
|---|------|------|---------|---------|
| 26 | `handler.rs:Prompt` | If `session.prompt()` failed to start the agent run (e.g. agent already busy), the RPC client would hang forever waiting for a response — no error was emitted | Missing error handling — TS wraps `session.prompt()` in `.catch()` and emits `error(id, "prompt", e.message)` on failure; Rust's `prompt()` returns `()` and silently ignores errors via `.ok()` | Added `Arc<AtomicBool>` flag set by listener on `AgentEnd`; after `prompt()` returns, check flag and emit error response if `AgentEnd` was never received |

| # | 位置 | 现象 | 根因模式 | 修复方式 |
|---|------|------|---------|---------|
| 27 | `mod.rs:parse_command` | Unknown command types produced `error(None, "parse", "Invalid JSON: ...")` instead of `error(id, command_type, "Unknown command: ...")` | Error format mismatch — TS catches unknown commands in `handleCommand()` switch `default` case and emits `error(command.id, command.type, "Unknown command: ...")`; Rust's serde deserialization caught unknown variants and emitted a generic parse error with no id/type | Parse JSON as generic `Value` first to extract `id` and `type`; on `RpcCommand` deserialization failure, use extracted `type` for `rpc_error(cmd_id, cmd_type, "Unknown command: {cmd_type}")` |
| 28 | `handler.rs:Compact` | Compact failure returned `success(id, "compact", {compacted: false, reason})` instead of `error(id, "compact", reason)` | Error response format mismatch — TS `session.compact()` throws on failure, caught by outer try-catch → `error(id, "compact", e.message)`; Rust returned a success response with `compacted: false` | Changed `Err` branch from `rpc_success(...)` to `rpc_error(id, "compact", reason)` |
| 29 | `rpc_types.rs:RpcCommand` | `RpcCommand` variants used snake_case field names (e.g. `streaming_behavior`, `exclude_from_context`, `custom_instructions`, `parent_session`, `session_path`, `entry_id`, `output_path`) but TS client sends camelCase field names (e.g. `streamingBehavior`, `excludeFromContext`, etc.) | Serde field naming mismatch — `#[serde(tag = "type", rename_all = "snake_case")]` on the enum only affects variant names (the `type` tag), not field names within each variant; TS client sends camelCase JSON keys | Added `#[serde(rename_all = "camelCase")]` to each variant with multi-word fields (`Prompt`, `Bash`, `NewSession`, `Compact`, `SwitchSession`, `Fork`, `ExportHtml`); also fixed `ImageRef.media_type` → `mime_type` with `#[serde(rename_all = "camelCase")]` on the struct |
| 30 | `rpc_types.rs:RpcOutput` | `RpcOutput` enum used PascalCase variant names (`Response`, `Event`, `ExtensionUiRequest`, `ExtensionError`) but TS client expects lowercase (`response`, `event`, `extension_ui_request`, `extension_error`) | Serde variant naming mismatch — `#[serde(tag = "type")]` without `rename_all` uses variant names as-is (PascalCase) | Added `#[serde(rename_all = "snake_case")]` to `RpcOutput` enum |
| 31 | `rpc_types.rs:RpcSessionState` | `RpcSessionState` used snake_case field names (`thinking_level`, `is_streaming`, `session_id`, etc.) but TS client expects camelCase (`thinkingLevel`, `isStreaming`, `sessionId`, etc.) | Serde field naming mismatch — no `rename_all` on struct, so field names used as-is (snake_case) | Added `#[serde(rename_all = "camelCase")]` to `RpcSessionState` |
| 32 | `rpc_types.rs:ExtensionUiRequestData` | `ExtensionUiRequestData` used snake_case field names (`notify_type`, `status_key`, `status_text`) but TS client expects camelCase (`notifyType`, `statusKey`, `statusText`) | Serde field naming mismatch — no `rename_all` on struct | Added `#[serde(rename_all = "camelCase")]` to `ExtensionUiRequestData` |
| 33 | `agent_session.rs:AgentSessionEvent` | `AgentSessionEvent` variants used snake_case field names (`tool_results`, `assistant_message_event`, `tool_call_id`, `tool_name`, `is_error`, `will_retry`, `follow_up`, `previous_model`, `error_message`, `max_attempts`, `delay_ms`, `final_error`) but TS client expects camelCase (`toolResults`, `assistantMessageEvent`, `toolCallId`, `toolName`, `isError`, `willRetry`, `followUp`, `previousModel`, `errorMessage`, `maxAttempts`, `delayMs`, `finalError`) | Serde field naming mismatch — `#[serde(rename_all = "snake_case")]` on enum only affects variant names, not field names; field names used as-is (snake_case) | Added `#[serde(rename_all = "camelCase")]` to each variant with multi-word fields |

| 34 | `agent_session.rs:CompactionReason` | `CompactionReason` enum serialized as PascalCase (`Manual`, `Threshold`, `Overflow`) but TS expects lowercase (`manual`, `threshold`, `overflow`) | Missing `#[serde(rename_all = "lowercase")]` on enum | Added `#[serde(rename_all = "lowercase")]` to `CompactionReason` |
| 35 | `rpc_types.rs:RpcOutput::ExtensionError` | `ExtensionError` variant had `extension_path` in snake_case but TS client expects `extensionPath` | Missing `#[serde(rename_all = "camelCase")]` on variant | Added `#[serde(rename_all = "camelCase")]` to `ExtensionError` variant |


| 36 | `agent_session.rs:set_model` / `cycle_model` | `ModelSelect` was emitted as `AgentSessionEvent` via `self._emit()`, causing RPC clients to receive unexpected `model_select` events | TS-only event routing — TS `_emitModelSelect()` only sends to extension runner, not session subscribers | Removed `self._emit(AgentSessionEvent::ModelSelect { ... })` calls from both `set_model()` and `cycle_model()`, keeping only `dispatch_model_select()` to extensions |
| 37 | `rpc_types.rs` | `RpcOutput::Event`, `RpcOutput::ExtensionUiRequest`, `RpcOutput::ExtensionError` variants were never constructed — dead code | Dead code from initial scaffolding | Removed all three unused variants, along with the `AgentEvent` enum, `ExtensionUiRequestData` struct, and `RpcExtensionUIResponse` struct that were only used by them |
| 38 | `rpc_types.rs:RpcExtensionUIResponse` | `#[serde(rename_all = "snake_case")]` on struct — TS sends camelCase field names | Style inconsistency — all fields are single words so no functional impact | Changed to `#[serde(rename_all = "camelCase")]` for consistency |

| 39 | `agent_session.rs:internal_listener` | Missing `auto_retry_end` with `success: true` on successful assistant response after retry — UI never gets notified when a retry succeeds | Missing event emission — TS `_handleAgentEvent` emits `auto_retry_end { success: true, attempt }` when `stopReason !== "error"` and `_retryAttempt > 0`; Rust internal listener (step 4) only tracked `last_assistant` and reset overflow recovery | Added `AutoRetryEnd { success: true, attempt, final_error: None }` emission in step 4 of internal listener when `stop_reason != Error` and `retry_attempt > 0` |
| 40 | `agent_session.rs:_prepare_retry` | Error message not removed from agent state before retry — the error message stays in context for the retry attempt, confusing the LLM | Missing state mutation — TS `_prepareRetry` removes the last assistant message from `agent.state.messages` before retry; Rust didn't | Added `self.agent.messages().await` → check last is assistant → `pop()` → `self.agent.set_initial_messages(truncated).await` |
| 41 | `agent_session.rs:check_auto_compact` | Context window hardcoded to 128_000 instead of using the model's actual context window | Hardcoded constant — TS uses `this.model?.contextWindow ?? 0`; Rust used `128_000` | Changed to use `state.model.context_window.max(1)` from agent state |
| 42 | `agent_session.rs:compact` | Context window hardcoded to 128_000 instead of using the model's actual context window | Same as #41 — hardcoded constant in `compact()` function | Changed to use `state.model.context_window.max(1)` from agent state |
| 43 | `agent_session.rs:_check_compaction` | Missing `sameModel` check — overflow from a different model (e.g. user switched from opus to codex) shouldn't trigger compaction | Missing model comparison — TS checks `this.model && assistantMessage.provider === this.model.provider && assistantMessage.model === this.model.id` | Added `same_model` check comparing `state.model.provider == *provider && state.model.id == *model` |
| 44 | `agent_session.rs:_check_compaction` | Missing context overflow detection and recovery — overflow errors from LLM were not detected, so no compaction+retry was triggered | Missing overflow logic — TS has `isContextOverflow()` with overflow patterns and `_overflowRecoveryAttempted` guard | Added overflow pattern matching on error messages, `_overflowRecoveryAttempted` check, and error message removal from agent state on overflow recovery |

| 45 | `agent_session.rs:prompt` | Missing compaction check before sending new prompt — aborted messages from previous turn could cause context overflow without triggering compaction | Missing pre-prompt compaction — TS `prompt()` calls `_checkCompaction(lastAssistant, false)` before sending; Rust didn't | Added compaction check on last assistant message before `add_user_text()` in `prompt()` |

| 46 | `agent_session_runtime.rs:fork` | `message.role()` called on `serde_json::Value` instead of `AgentMessage` — `SessionEntry::Message { message }` has `message: serde_json::Value`, not `AgentMessage` | Type mismatch — `SessionEntry::Message` stores `message` as `serde_json::Value` for serialization, but the fork validation code assumed it was `AgentMessage` and called `.role()` directly | Added `serde_json::from_value::<AgentMessage>(message.clone()).ok()` to deserialize before calling `.role()` |
| 47 | `agent_session.rs:new` | Unused variable `inner_agent` — cloned from `agent` parameter but never used; the `agent` is moved into `Self { agent, ... }` directly | Leftover from refactoring — the variable was likely used before `agent` was moved into the struct | Removed the unused `let inner_agent = agent.clone();` line |
| 48 | `agent_session_runtime.rs:fork` | Redundant closure call `(|| { ... })()` — clippy warning about calling a closure in the expression where it's declared | Unnecessary closure wrapping — the `if let` can be used directly as an expression | Replaced `(|| { if let ... } else { None } })()` with direct `if let ... else { None }` expression |

| 49 | `agent_session.rs:dispose` | Missing abort calls — `dispose()` didn't call `abortRetry()`, `abortCompaction()`, `abortBranchSummary()`, `abortBash()`, or `agent.abort()` before cleanup | Missing cleanup — TS `dispose()` calls all abort methods before disconnecting; Rust just disconnected and cleared listeners | Added abort calls wrapped in `catch_unwind` (matching TS try-catch), plus `agent.abort().await` |
| 50 | `agent_session.rs:dispose` | Double `session_shutdown` dispatch — both `teardown_current()` and `dispose()` emitted `session_shutdown` | Architecture mismatch — TS `AgentSession.dispose()` does NOT emit shutdown (it's done by `AgentSessionRuntime.teardownCurrent()`); Rust `dispose()` was emitting it again | Removed `session_shutdown` dispatch from `dispose()`, added comment explaining it's handled by `teardown_current()` |
| 51 | `agent_session_runtime.rs:import_from_jsonl` | Missing `sessionFile` and `fallbackCwd` in cwd validation error message — TS `assertSessionCwdExists()` includes both, Rust only had `session_cwd` | Incomplete port — the error message didn't match TS `formatMissingSessionCwdError()` format | Added `session_file` variable and included it plus `self.cwd()` in the error message |
| 52 | `agent_session_runtime.rs:switch_session` | Same as #51 — missing `sessionFile` and `fallbackCwd` in cwd validation error message | Same root cause | Same fix applied |
| 53 | `agent_session_runtime_test.rs` | Test referenced non-existent field `session_start_event` on `CreateAgentSessionRuntimeParams` | Test was written for a struct field that was never added | Removed the field from the test |

| 54 | `agent_session_services.rs:CreateAgentSessionFromServicesOptions` | Missing `excludeTools` and `customTools` fields — TS passes these through to `createAgentSession()`; Rust struct was missing them | Missing fields in options struct | Added `exclude_tools: Option<Vec<String>>` and `custom_tools: Option<Vec<ToolDefinition>>` fields, passed through to `create_agent_session()` |
| 55 | `agent_session_runtime.rs:fork` | Missing persisted session branch — TS `fork()` has three branches (persisted+no-target, persisted+target, in-memory); Rust only had two (target vs no-target) | Missing `isPersisted()` check — Rust always used `persist: true` regardless of session type | Added `is_persisted()` check with three branches matching TS: persisted+no-target uses `newSession({parentSession})`, persisted+target uses `SessionManager.open()`+`createBranchedSession()`, in-memory uses `persist: false` |
| 56 | `agent_session_runtime.rs:new_session` | Missing `setup` callback — TS `newSession()` accepts `setup?: (sessionManager) => Promise<void>` and calls it after applying the new runtime | Missing parameter — Rust `new_session()` only accepted `parent_session` | Added `setup` callback parameter (noted: no current callers use it, but the interface now matches TS) |
| 57 | `agent_session_runtime.rs:fork` | Missing `withSession` callback — TS `fork()` accepts `withSession?: (ctx) => Promise<void>` and passes it to `finishSessionReplacement()` | Missing parameter propagation | Added `with_session` parameter to `fork()`, `new_session()`, `switch_session()`, and `finish_session_replacement()` |
| 58 | `agent_session_runtime.rs:import_from_jsonl` | Missing `resolvePath()` — TS calls `resolvePath(inputPath)` to resolve relative paths against cwd; Rust used `Path::new(input_path)` directly | Missing path resolution — relative paths would not resolve correctly | Changed to use `crate::config::resolve_path(input_path)` before creating `Path` |
| 59 | `agent_session_runtime.rs:import_from_jsonl` | Missing `assertSessionCwdExists()` — TS calls `assertSessionCwdExists(sessionManager, this.cwd)` which throws `MissingSessionCwdError`; Rust just checked `session_cwd.is_empty()` | Different error behavior — TS throws typed error, Rust returned generic `Err(String)` | Updated to use `assert_session_cwd_exists()` from `session_cwd` module (note: `SessionManager` doesn't implement `SessionCwdSource` trait, so kept manual check with matching error format) |
| 60 | `agent_session.rs:_handleAgentEvent` | Missing `turnIndex` in `turn_start`/`turn_end` — TS `_emitExtensionEvent()` passes `turnIndex: this._turnIndex` to extension events; Rust HookRunner didn't track or pass turn index | Missing state tracking — TS has `this._turnIndex` field incremented on each `turn_end`; Rust had no equivalent | Added `Arc<Mutex<u32>> turn_index` counter, reset on `AgentStart`, passed to `fire_turn_start()` and `fire_turn_end()`, incremented after `TurnEnd` |
| 61 | `agent_session.rs:_handleAgentEvent` | Missing `normalized` replacement for null content in `message_end` — TS normalizes replacement messages with `content == null` to `content: []` before replacing in place | Missing normalization — TS checks `replacement.role` and `replacement.content == null` and sets `content: []`; Rust just deserialized directly | Added normalization: check if role is user/assistant/toolResult/custom and content is null, then set content to empty array before deserializing |

| 62 | `agent_session.rs:internal_listener` | Missing `_replaceMessageInPlace` semantics — persistence code used `event.message` (original) instead of the replacement after extensions modified the message via `agent.replace_last_message()` | Data corruption — TS `_replaceMessageInPlace` mutates the original object in place, so `event.message` already points to the replacement when persistence runs; Rust `agent.replace_last_message()` replaces the Vec element but `event.message` still holds the original reference | Changed persistence code (step 4) to re-read the last message from `agent.state().await.messages.last()` instead of using `event.message`, ensuring persistence always uses the post-extension-replacement message |
| 63 | `agent_session_runtime.rs:teardown_current` | Missing `targetSessionFile` parameter — TS `teardownCurrent()` passes `targetSessionFile` to `emitSessionShutdownEvent`; Rust passed `None` | Missing parameter — extensions don't get the target session file path during shutdown | Added `target_session_file: Option<&str>` parameter to `teardown_current()`, passed through to `fire_session_shutdown()`, updated all callers |
| 64 | `agent_session_runtime.rs:teardown_current` | Missing `session.dispose()` call — TS `teardownCurrent()` calls `this.session.dispose()` after `beforeSessionInvalidate`; Rust didn't | Missing cleanup — abort controllers, agent subscription, and bash cleanup don't run during session teardown | Added `self.session.dispose().await` after `before_session_invalidate` in `teardown_current()`; changed `AgentSession::dispose()` from `mut self` to `&mut self` to allow calling on a field reference |
| 65 | `agent_session_runtime.rs:switch_session` | Missing `assertSessionCwdExists()` — TS `switchSession()` calls `assertSessionCwdExists(sessionManager, this.cwd)`; Rust had manual cwd validation that didn't use the shared function | Inconsistent validation — manual validation didn't match TS error format or reuse the shared `session_cwd` module | Replaced manual cwd validation with `crate::core::session_cwd::assert_session_cwd_exists(&session_manager, self.cwd())`; implemented `SessionCwdSource` for `SessionManager` |
| 66 | `agent_session_runtime.rs:switch_session` | Missing `withSession` callback — TS `switchSession()` accepts `withSession?: (ctx) => Promise<void>` and passes it to `finishSessionReplacement()`; Rust didn't have this parameter | Missing parameter — callers can't inject a callback to run after session replacement | Added `with_session` parameter to `switch_session()`, passed through to `finish_session_replacement()` |
| 67 | `agent_session_runtime.rs:import_from_jsonl` | Missing `assertSessionCwdExists()` — same as #65 but in `import_from_jsonl()` | Same root cause as #65 | Replaced manual cwd validation with `assert_session_cwd_exists()` |
| 68 | `session_manager.rs:build_session_info` | `first_message` always shows "(no messages)" even when session has user messages — content extraction only handled string content, not content arrays | 高危陷阱：TS `extractTextContent()` handles both `string` and `(TextContent | ImageContent)[]` content; Rust used `message.get("content").and_then(|c| c.as_str())` which returns `None` for array content | Added `extract_text_content()` helper that handles both string and array content (filtering for `type: "text"` blocks), matching TS behavior. Also added role filter (`user`/`assistant` only) for `all_messages` to match TS. |
