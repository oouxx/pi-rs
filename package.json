{
  "name": "pi-rs",
  "version": "1.78.0",
  "private": true,
  "description": "Rust port of pi — coding agent framework",
  "workspaces": [
    "rpc-host"
  ],
  "scripts": {
    "start:rpc-host": "bun run rpc-host/src/index.ts",
    "build:rpc-host": "cd rpc-host && bun install"
  },
  "dependencies": {
    "@hypabolic/pi-hypa": "^0.1.6"
  }
}
